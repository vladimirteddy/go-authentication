apiVersion: v1
kind: Service
metadata:
  name: go-authentication
  labels:
    app: go-authentication
spec:
  selector:
    app: go-authentication
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
